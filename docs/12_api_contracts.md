# 12. Contratos de API
N/A — No tiene API.
Los contratos son las interfaces de props de cada componente. Ejemplo:
```typescript
interface HeroSectionProps {
  title: string
  subtitle?: string
  ctaText: string
  ctaHref: string
  backgroundImage?: string
}
```
